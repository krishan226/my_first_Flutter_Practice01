//  value Notifier ---  Hold the data
//  value listenableBuilder // listen the data (don't need then SetState)

import 'package:flutter/material.dart';

ValueNotifier<int> selectedPageNotifier = ValueNotifier(0);
